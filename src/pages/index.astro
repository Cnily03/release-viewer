---
import config from "@/config";
import Layout from "@/layouts/Layout.astro";
import FileContainer from "@/components/FileContainer.astro";
---

<Layout title={config.name} class:list="p-8">
  <FileContainer repoUrl={config.repo_url} release={config.releases[10]!} defaultOpen />
</Layout>
